### 20260107

https://leetcode.cn/problems/degree-of-an-array/?envType=problem-list-v2&envId=array

```java
class Solution {
    public int findShortestSubArray(int[] nums) {

        if (nums.length == 1) {
            return 1;
        }

        int max;
        int result;
        Map<Integer, List<Integer>> m = new HashMap<>();

        List<Integer> r = new ArrayList<>();
        r.add(0);
        r.add(0);
        r.add(1);
        r.add(0-0+1);
        m.put(nums[0], r);
        max = 1;
        result = nums[0];

        for (int i = 1; i < nums.length; i++) {
            r = m.get(nums[i]);
            if (r == null) {
                r = new ArrayList<>();
                r.add(i);
                r.add(i);
                r.add(1);
                r.add(i - i +1);
                m.put(nums[i], r);
            } else {
                List<Integer> r1 = new ArrayList<>();
                r1.add(r.get(0));
                r1.add(i);
                r1.add(r.get(2) + 1);
                r1.add( i - r1.get(0) + 1);
                if(r1.get(2) > max){
                    result = nums[i];
                    max = r1.get(2);
                }else if (r1.get(2) == max && r1.get(3) < m.get(result).get(3)){
                    result = nums[i];
                }

                m.put(nums[i], r1);
            }
        }

        r = m.get(result);
        return r.get(1) - r.get(0) + 1;
    }
}
```

```java
class Solution {
    public int findShortestSubArray(int[] nums) {
        Map<Integer, int[]> map = new HashMap<Integer, int[]>();
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            if (map.containsKey(nums[i])) {
                map.get(nums[i])[0]++;
                map.get(nums[i])[2] = i;
            } else {
                map.put(nums[i], new int[]{1, i, i});
            }
        }
        int maxNum = 0, minLen = 0;
        for (Map.Entry<Integer, int[]> entry : map.entrySet()) {
            int[] arr = entry.getValue();
            if (maxNum < arr[0]) {
                maxNum = arr[0];
                minLen = arr[2] - arr[1] + 1;
            } else if (maxNum == arr[0]) {
                if (minLen > arr[2] - arr[1] + 1) {
                    minLen = arr[2] - arr[1] + 1;
                }
            }
        }
        return minLen;
    }
}
```

```java
class Solution {
    public int findShortestSubArray(int[] nums) {

        if (nums.length == 1) {
            return 1;
        }

        int max;
        int result;
        Map<Integer, int[]> m = new HashMap<>();

        int[] r = new int[]{0,0,1,1};
        m.put(nums[0], r);
        max = 1;
        result = nums[0];

        for (int i = 1; i < nums.length; i++) {
            r = m.get(nums[i]);
            if (r == null) {
                r = new int[]{i,i,1,1};
                m.put(nums[i], r);
            } else {
                int[] r1 = new int[]{r[0],i,r[2]+1,i - r[0]+1};
                if(r1[2] > max){
                    result = nums[i];
                    max = r1[2];
                }else if (r1[2] == max && r1[3] < m.get(result)[3]){
                    result = nums[i];
                }

                m.put(nums[i], r1);
            }
        }

        r = m.get(result);
        return r[3];
    }
}
```
