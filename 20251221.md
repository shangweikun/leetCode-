### 20251221

https://leetcode.cn/problems/find-all-numbers-disappeared-in-an-array/submissions/686130712/?envType=problem-list-v2&envId=array

```java
class Solution {
    public List<Integer> findDisappearedNumbers(int[] nums) {
        List<Integer> result = new ArrayList<>();
        for(int i = nums.length - 1 ; i >=0 ; ){
            if ( nums[i] == (i+1) ){
                i--;
            } else if (nums[i] == nums[(nums[i]-1)]){
                i--;
            } else {
                int tmp = nums[(nums[i]-1)];
                nums[(nums[i]-1)] =  nums[i];
                nums[i] = tmp;
            }
        }

        for(int i = 0 ; i < nums.length; i++){
            if(nums[i] != (i+1)){
                result.add(i+1);
            }
        }

        return result;
    }
}
```

```java
class Solution {
    public List<Integer> findDisappearedNumbers(int[] nums) {
        List<Integer> result = new ArrayList<>();
        for(int i = nums.length - 1 ; i >=0 ; ){
            if ( nums[i] == (i+1) || nums[i] == nums[(nums[i]-1)]){
                i--;
            } else {
                int tmp = nums[(nums[i]-1)];
                nums[(nums[i]-1)] =  nums[i];
                nums[i] = tmp;
            }
        }

        for(int i = 0 ; i < nums.length; i++){
            if(nums[i] != (i+1)){
                result.add(i+1);
            }
        }

        return result;
    }
}
```

```java
class Solution {
    public List<Integer> findDisappearedNumbers(int[] nums) {
        int n = nums.length;
        for (int num : nums) {
            int x = (num - 1) % n;
            if (nums[x] <= n) {
                nums[x] += n;
            }
        }
        List<Integer> ret = new ArrayList<Integer>();
        for (int i = 0; i < n; i++) {
            if (nums[i] <= n) {
                ret.add(i + 1);
            }
        }
        return ret;
    }
}
```

