### 20260115

https://leetcode.cn/problems/baseball-game/?envType=problem-list-v2&envId=array

```java
class Solution {
    public int calPoints(String[] operations) {
        int result = 0;
        Stack<Integer> stack = new Stack<>();
        for (int i = 0; i < operations.length; i++) {
            String operation = operations[i];
            if ("C".equals(operation)) {
                stack.removeLast();
            } else if ("D".equals(operation)) {
                stack.add(2 * stack.getLast());
            } else if ("+".equals(operation)) {
                stack.add(stack.get(stack.size() - 1) + stack.get(stack.size() - 2));
            } else {
                stack.add(Integer.valueOf(operations[i]));
            }
        }
        for (Integer i1Integer : stack) {
            result += i1Integer;
        }

        return result;
    }
}
```
